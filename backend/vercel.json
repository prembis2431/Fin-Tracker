{
    "version": 2,
    
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/",
        "methods": ["GET", "POST", "DELETE", "PUT", "PATCH", "OPTIONS"],
        "headers": {

          "Access-Control-Allow-Origin": "*"
        }
      }
    ]
  }